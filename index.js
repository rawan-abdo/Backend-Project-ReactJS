const express = require('express') // require statement has imported the express function from the express package (which was installed 'npm install express')
const app = express()

app.listen(5000, () => {
    
})